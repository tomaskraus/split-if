{
  "name": "split-if",
  "version": "1.0.4",
  "description": "RxJS operator. Packs all the source Observable values into arrays. New array is emitted every time a predicate is fulfilled.",
  "types": "build/src/splitif.d.ts",
  "main": "build/src/splitif.js",
  "files": [
    "build/src"
  ],
  "author": "Tomas Kraus <tomas.kraus.dev@gmail.com>",
  "license": "MIT",
  "keywords": [
    "predicate",
    "array",
    "split",
    "partition",
    "partitionN",
    "partitionBy",
    "operator",
    "rxjs",
    "typescript",
    "ts",
    "functional",
    "fp"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/tomaskraus/split-if"
  },
  "scripts": {
    "test": "jest --coverage",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "@types/jest": "^28.1.6",
    "@types/node": "^20.1.0",
    "gts": "^5.0.1",
    "jest": "^28.0.7",
    "rxjs": "^7.5.7",
    "rxjs-marbles": "^7.0.1",
    "ts-jest": "^28.0.7",
    "typescript": "~5.3.3"
  }
}
